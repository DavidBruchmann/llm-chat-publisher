/**
 * Project-wide custom settings
 *
 * This file is:
 * - generated by the admin UI
 * - consumed by build scripts
 * - safe to commit and version
 */

const settings = {
  branding: {
    logo: {
      file: null,           // e.g. "static/img/logo.svg"
      alt: "Site logo"
    }
  },

  typography: {
    fontFamily: "system-ui",
    baseFontSize: "16px"
  },

  colors: {
    mode: "auto",           // "light" | "dark" | "auto"

    light: {
      primary: "#2563eb",
      text: "#0f172a",
      background: "#ffffff"
    },

    dark: {
      primary: "#3b82f6",
      text: "#e5e7eb",
      background: "#020617"
    }
  },

  kofi: {
    username: null,         // e.g. "yourname"
    imageLight: "static/img/kofi-black.png",
    imageDark: "static/img/kofi-white.png",
    aspectRatio: "980 / 198"
  },

  meta: {
    defaultLanguage: "en",
    enableI18n: false
  }
};

export default settings;
